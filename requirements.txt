numpy>=1.26,<3
pandas>=2.2,<3
requests>=2.32,<3
torch>=2.4,<3
tzdata>=2024.1
# Install cuDF, cuML and compatible CuPy separately in Linux/WSL2:
# https://docs.rapids.ai/install/
# Match the RAPIDS release and CUDA version to the NVIDIA cluster image.
